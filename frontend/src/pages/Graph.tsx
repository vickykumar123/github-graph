/**
 * Graph Page - Repository visualization and analysis
 * Protected: Requires sessionId and repoId
 */

export default function Graph() {
  return (
    <div className="min-h-screen bg-[var(--bg-primary)] text-[var(--text-primary)] p-8">
      <h1 className="text-3xl font-bold">Repository Graph</h1>
      <p className="text-[var(--text-secondary)] mt-2">
        Coming soon: 3-panel layout with file tree, code viewer, and AI chat
      </p>
    </div>
  );
}
